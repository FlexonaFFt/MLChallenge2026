from dataclasses import dataclass, field

SYSTEM_PROMPT = (
    "Ты — профессиональный репетитор с глубокими знаниями по всем школьным "
    "предметам: математике, физике, химии, русскому и иностранным языкам, "
    "литературе, биологии и истории.\n\n"
    "Стандарты ответа:\n"
    "- Отвечай сразу по существу — без приветствий и вводных фраз\n"
    "- Давай полный, законченный ответ — не обрывай на полуслове\n"
    "- Объясняй не только что, но и почему — показывай ход рассуждений\n"
    "- Используй правильные обозначения: формулы, дроби, схемы разбора\n\n"
    "Формат по типу задания:\n"
    "- Математика и физика: шаги решения → проверка → выделенный ответ\n"
    "- Химия: уравнение с расставленными коэффициентами и условиями реакции\n"
    "- Разбор слова или предложения: точная схема по школьной программе\n"
    "- Сочинение или текст: полный текст — вступление, основная часть, заключение\n"
    "- Грамматика: правило → примеры → исключения (если есть)\n\n"
    "Правила оформления:\n"
    "- Формулы и числовые выражения — в LaTeX ($...$), выносные — в $$...$$.\n"
    "- Десятичные дроби — с запятой в LaTeX: $7{,}5$ (не 7.5).\n"
    "- Где есть однозначный итог — заверши строкой «**Ответ:** ...».\n"
    "- Списки оформляй через дефис «- ».\n"
    "- Разбор слова/предложения — давай чёткую схему по школьной программе.\n\n"
    "Чего НЕ делать:\n"
    "- Не повторяй и не переписывай условие вопроса.\n"
    "- Без приветствий, вступлений, извинений и фраз вроде «как ИИ».\n"
    "- Никаких мета-комментариев о себе или о процессе — только сам ответ.\n\n"
    "Отвечай на языке вопроса."
)

# Минимальный промпт — для дообученной модели (стиль приходит из весов).
# ВАЖНО: должен дословно совпадать с MINIMAL_SYSTEM в effinf_dev/prepare_data.py.
MINIMAL_SYSTEM = "Отвечай на языке вопроса."

# Few-shot примеры в формате эталонов (показ формата сильнее инструкций).
# Общий префикс кэшируется (prefix caching), поэтому почти бесплатно по времени.
FEW_SHOT: list[tuple[str, str]] = [
    (
        "Вычислите значение выражения 6032 − 6012.",
        "Вычислим значение выражения $6032 - 6012$:\n\n"
        "$$\n6032 - 6012 = 20\n$$\n\n**Ответ:** $20$",
    ),
    (
        "в слове «медовый» где корень, суффикс, окончание?",
        "Разберём слово **«медовый»** по составу:\n\n"
        "- **корень**: *мед-* (от слова «мёд»);\n"
        "- **суффикс**: *-ов-* (образует прилагательное от существительного);\n"
        "- **окончание**: *-ый* (мужской род, единственное число, именительный падеж).\n\n"
        "Схема: **мед‑ов‑ый** (корень — суффикс — окончание).",
    ),
    (
        "уравнение реакции горения метана в кислороде",
        "Уравнение реакции горения метана ($\\text{CH}_4$) в кислороде "
        "($\\text{O}_2$) с образованием углекислого газа и воды:\n\n"
        "$$\n\\text{CH}_4 + 2\\text{O}_2 \\to \\text{CO}_2 + 2\\text{H}_2\\text{O}\n$$",
    ),
    (
        "Раскрой скобки в Present Perfect: I (read) a new book.",
        "**I have read a new book.**\n\n"
        "**Объяснение:** Present Perfect образуется как *have/has* + причастие "
        "прошедшего времени. У глагола *read* форма причастия совпадает с "
        "инфинитивом — *read*.",
    ),
]


@dataclass
class InferenceConfig:
    model_dir: str = "./weights"
    max_new_tokens: int = 512   # ниже кэп → запас по времени для 8B (де-риск TL)
    max_model_len: int = 2048   # ~950 префикс + 512 генерация < 2048
    gpu_memory_utilization: float = 0.9
    temperature: float = 0.0
    dtype: str = "float16"   # AWQ-модель → fp16 compute (для bf16-модели ставить "bfloat16")
    # Speculative decoding: черновик 0.6B ускоряет декод 8B (то же качество).
    # "" — выключить. num_speculative_tokens — сколько токенов предлагает черновик.
    spec_draft_model: str = "./draft"
    num_speculative_tokens: int = 5
    system_prompt: str = field(default=SYSTEM_PROMPT)
    few_shot: list[tuple[str, str]] = field(default_factory=lambda: FEW_SHOT)
    # Динамический few-shot убивает prefix caching → 22 мин на 4000 (TL). ВЫКЛ.
    # Оставлено в коде на случай меньшего масштаба/более быстрой модели.
    dynamic_few_shot: bool = False
    few_shot_k: int = 2                      # сколько примеров подставлять
    few_shot_max_answer_chars: int = 900     # кап длины примера (ограничить префикс/время)
    enable_prefix_caching: bool = True
    max_num_seqs: int = 256
